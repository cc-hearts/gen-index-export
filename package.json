{
  "name": "@cc-heart/gen-index-export",
  "version": "0.0.7",
  "description": "Generate the exported statement",
  "main": "index.js",
  "type": "module",
  "bin": "dist/bin/index.js",
  "scripts": {
    "prebuild": "rimraf dist",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "build": "npm run prebuild && tsc -p tsconfig.build.json && rollup -c",
    "playground": "ts-node-esm index.ts --path ./example/a ./example/b --output ./example/a/index.ts ./example/b/test.ts --ignoreIndexPath",
    "argv": "ts-node-esm example/argv.ts --path ./example ./asd --output ./example/index.ts ./example/test.ts --ignoreIndexPath",
    "deploy": "npm run prebuild && npm run build",
    "prettier": "npx prettier --write ."
  },
  "keywords": [
    "export"
  ],
  "author": "heart",
  "license": "MIT",
  "dependencies": {
    "@babel/parser": "^7.22.4",
    "@babel/traverse": "^7.22.4",
    "@cc-heart/utils": "^2.0.12",
    "glob": "^9.3.5"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.0.2",
    "@rollup/plugin-typescript": "^11.1.0",
    "@types/node": "^18.16.2",
    "@rollup/plugin-commonjs": "^24.1.0",
    "@types/babel__traverse": "^7.20.1",
    "rimraf": "^4.4.1",
    "tslib": "^2.5.0",
    "vitest": "^0.29.8"
  }
}